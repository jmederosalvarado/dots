- name: Install required packages from official repos
  pacman:
    name:
      - git
      - docker
      - docker-compose
      - python
      - nodejs
      - npm
      - yarn
      - github-cli
      - tmux
    state: present
  become: yes

- name: Install required packages from aur
  aur:
    use: paru
    name:
      - gitlab-glab-bin
